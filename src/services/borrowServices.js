const borrowBook = async (memberId, bookId) => {
  // Check if the member has borrowed less than 2 books
  // Check if the book is available
  // Check if the member is penalized
  // Update the borrow records
};

module.exports = { borrowBook };
